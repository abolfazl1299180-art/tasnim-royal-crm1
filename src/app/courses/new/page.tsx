"use client";
import {FormEvent,useState} from "react";import{useRouter}from"next/navigation";import{createClient}from"@/lib/supabase/browser";
export default function NewCoursePage(){const router=useRouter();const[saving,setSaving]=useState(false);const[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);setError("");const f=new FormData(e.currentTarget);const{error}=await createClient().from("courses").insert({title:String(f.get("title")||"").trim(),description:String(f.get("description")||"").trim()||null,start_date:f.get("start_date")||null,end_date:f.get("end_date")||null,capacity:f.get("capacity")?Number(f.get("capacity")):null,price:Number(f.get("price")||0),is_active:f.get("is_active")==="true"});if(error){setError("ثبت دوره انجام نشد.");setSaving(false);return}router.push("/courses");router.refresh()}return <div className="max-w-2xl"><h1 className="text-2xl font-black">دوره جدید</h1><p className="mt-2 text-sm text-slate-500">تعریف دوره و ظرفیت ثبت‌نام</p><form onSubmit={submit} className="mt-6 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="grid gap-4"><label><span className="mb-2 block text-sm font-bold">عنوان دوره</span><input name="title" required className="w-full rounded-2xl border p-3"/></label><label><span className="mb-2 block text-sm font-bold">توضیحات</span><textarea name="description" rows={3} className="w-full rounded-2xl border p-3"/></label><div className="grid gap-4 sm:grid-cols-2"><label><span className="mb-2 block text-sm font-bold">شروع</span><input name="start_date" type="date" className="w-full rounded-2xl border p-3"/></label><label><span className="mb-2 block text-sm font-bold">پایان</span><input name="end_date" type="date" className="w-full rounded-2xl border p-3"/></label><label><span className="mb-2 block text-sm font-bold">ظرفیت</span><input name="capacity" type="number" min="1" className="w-full rounded-2xl border p-3"/></label><label><span className="mb-2 block text-sm font-bold">هزینه (تومان)</span><input name="price" type="number" min="0" required className="w-full rounded-2xl border p-3"/></label></div><label><span className="mb-2 block text-sm font-bold">وضعیت</span><select name="is_active" defaultValue="true" className="w-full rounded-2xl border p-3"><option value="true">فعال</option><option value="false">غیرفعال</option></select></label></div>{error&&<p className="mt-4 text-sm font-bold text-red-600">{error}</p>}<div className="mt-5 flex gap-3"><button type="button" onClick={()=>router.back()} className="rounded-2xl border px-5 py-3 font-bold">انصراف</button><button disabled={saving} className="rounded-2xl bg-slate-950 px-6 py-3 font-black text-white">{saving?"در حال ثبت...":"ثبت دوره"}</button></div></form></div>}
