import Link from "next/link";
import { createClient } from "@/lib/supabase/server";
import { PageHeader } from "@/components/ui/page-header";
const labels:Record<string,string>={pending:"در انتظار",done:"انجام‌شده",cancelled:"لغوشده"};
export default async function FollowUpsPage(){const supabase=await createClient();const {data:items}=await supabase.from("follow_ups").select("id,title,due_at,status,notes,contacts(first_name,last_name)").order("due_at",{ascending:true}).limit(50);return <><PageHeader title="پیگیری‌ها" description="پیگیری ارتباطات و سرنخ‌های آینده" action="پیگیری جدید" actionHref="/follow-ups/new"/><div className="grid gap-4">{items?.map(item=><article key={item.id} className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-black">{item.title}</h2><p className="mt-1 text-sm text-slate-500">{Array.isArray(item.contacts)?"مخاطب":`${item.contacts?.first_name||""} ${item.contacts?.last_name||""}`}</p></div><span className="rounded-full bg-amber-50 px-3 py-1 text-xs font-bold text-amber-700">{labels[item.status]||item.status}</span></div><p className="mt-4 text-sm text-slate-400">{new Date(item.due_at).toLocaleString("fa-IR")}</p></article>)}</div>{!items?.length&&<div className="rounded-3xl bg-white p-12 text-center text-sm text-slate-400">پیگیری فعالی وجود ندارد.</div>}<Link href="/contacts" className="mt-6 inline-block text-sm font-bold text-amber-600">مشاهده مخاطبین ←</Link></>}
